import React from 'react';
import { ScreenProps } from '@frontend/domain/cancellation-flow/interfaces/component-props';

// This component is used to display the custom footer for the keep summer access screen
export const CustomFooterKeepSummerAccess = (props: ScreenProps) => {
  return <></>;
};
